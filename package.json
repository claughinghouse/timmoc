{
  "$schema": "https://www.schemastore.org/package.json",
  "name": "timmoc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write --unsafe",
    "update:all": "bunx npm-check-updates -ui -ws",
    "cf-typegen": "wrangler types",
    "check": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "17.2.3",
    "drizzle-orm": "0.44.7",
    "wrangler": "4.50.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20251126.0",
    "drizzle-kit": "0.31.7"
  }
}
