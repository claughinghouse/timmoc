{
  "name": "timmoc",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "format:check": "prettier --check \"**/*.{ts,tsx,mdx}\" --cache",
    "format:write": "prettier --write \"**/*.{ts,tsx,mdx}\" --cache",
    "sso": "aws sso login --profile development",
    "sst-dev": "sst dev",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "hono": "^4.2.3"
  }
}
